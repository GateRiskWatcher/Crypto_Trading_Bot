# =============================================================
# 复制本文件为 ../secrets/.env 并填入你的只读 API key
# 或直接运行 python src/setup_keys.py 交互输入（推荐）
#
# 重要：
#  - 只用“只读”权限的 API key（不要勾任何交易/提现权限）
#  - 本文件不入库、不含真值；真实 key 写在 secrets/.env
#  - key 每 90 天过期（未绑定 IP），到期只需重跑 setup_keys.py
#
# 支持多账户：account_1 用 GATE_API_KEY / GATE_API_SECRET
#            account_2 用 GATE_API_KEY_2 / GATE_API_SECRET_2
#            account_3 用 GATE_API_KEY_3 / GATE_API_SECRET_3 ...
# 系统按实际存在的 KEY 数量自动决定监控几个账户
# =============================================================

GATE_API_KEY=your_readonly_key_here
GATE_API_SECRET=your_readonly_secret_here

# GATE_API_KEY_2=your_second_readonly_key_here
# GATE_API_SECRET_2=your_second_readonly_secret_here
